# Loads data from the archive

extract_archive <- function(commit, fl){
  #' Extracts the file from an archive 
  #' @param commit the commit id
  #' @param fl the file name to extract
 
  
  dir <- dirname(commit)
  hash <- 
  
  
}
